const Configurations = {
    // LOOP Matrix: main font family (Poppins). Font files are self-hosted via site/public/css/main.css
    "typography": {
      "fontFamily": "'Poppins', 'Open Sans', 'Helvetica', 'Arial', sans-serif"
    },
    "custom": {
      "landingPage": {
        "active": true
      },
      // LOOP Matrix header theme overrides (runtime-loaded, no rebuild needed)
      "appBar": {
        "background": "#141A21",        // header background color
        "backgroundImage": "",          // remove default appbarBack.png overlay
        "activeBackground": "#FF5F00"    // accent / active color
      }
    }
  };
